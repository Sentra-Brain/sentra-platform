/* tsconfig.json */
// Root config: delegates to app (React) and node (Vite config)
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
