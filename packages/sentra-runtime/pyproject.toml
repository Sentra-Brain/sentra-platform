[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentra-runtime"
version = "0.0.0"
description = "Sentra runtime package"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-adk",
    "pydantic",
    "anyio",
    "pytest",
    "ddgs",
    "litellm"
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
