---
const { icon = "🧩", title, desc } = Astro.props;
---

<div class="bg-[var(--sentra-neutral)] p-6 rounded-2xl border-2 border-[var(--sentra-accent-light)]  text-[var(--sentra-primary)] shadow-sentra transition-all duration-300 hover:shadow-lg flex flex-col items-center text-center">
  <div class="text-4xl mb-4">{icon}</div>
  <h3 class="text-lg md:text-xl font-semibold mb-2">{title}</h3>
  <p class="text-sm md:text-base">{desc}</p>
</div>
