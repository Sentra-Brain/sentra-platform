{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Admin",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 9329,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/app",
            "skipFiles": ["<node_internals>/**"],
        }
    ]
}
